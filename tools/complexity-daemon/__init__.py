"""
Complexity Daemon (cogload) - Cumulative complexity auto-commit tool.

A local daemon that monitors file changes, tracks cumulative complexity delta,
and auto-commits when a threshold is reached.
"""

__version__ = "0.1.0"
